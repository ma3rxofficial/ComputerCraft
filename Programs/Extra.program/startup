-- ДЛЯ ТЕХ КТО НЕ РАЗОБРАЛСЯ ПОСМОТРИТЕ ВИДЕО ПРО КРАШЕР СЕРВЕРОВ А НЕ ЕБИТЕ МНЕ ЛС ВОПРОСАМИ!!! :D

os.loadAPI("loader")
SpeedOS = loader.OS()

SpeedOS.ToolBarColour = colours.black
SpeedOS.ToolBarTextColour = colours.white

print("Press Enter to start the Pizdec")
print("Place a redstone torch next to right side of redstone")
os.pullEvent("key")

while true do
  if rs.getInput("back", false) then
    rs.setOutput("back", true)
  end
  
  if rs.getInput("right", true)  then
    rs.setOutput("front", true)
    rs.setOutput("left", false)
  end

  if rs.getInput("right", false) then
    rs.setOutput("left", true)
  end
end
