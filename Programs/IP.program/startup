SpeedOS.LoadAPI("SpeedAPI/windows")
SpeedOS.LoadAPI("SpeedAPI/SpeedText")

SpeedOS.ToolBarColour = colours.lightGrey
SpeedOS.ToolBarTextColour = colours.white

windows.clearScreen(colors.gray)
term.setTextColor(colors.white)

local test = http.get("https://api.ipify.org/?format=raw")

if not test then
	windows.clearScreen(colors.white)
	windows.error("Can't reach IP server!")
	SpeedOS.Close()
end

SpeedText.cPrint("Your IP is "..test.readAll())

SpeedText.waitForEvent({"mouse_click", "mouse_drag", "key"})
SpeedOS.Close()
