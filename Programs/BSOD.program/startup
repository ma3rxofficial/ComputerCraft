-----------------------------------------------------------------------------------------------------------------------------------
-- Меняем цвет тулбара

if SpeedOS then
	SpeedOS.ToolBarColour = colours.white
	SpeedOS.ToolBarTextColour = colours.black
end

-----------------------------------------------------------------------------------------------------------------------------------
-- Подгружаем нужные программке АПИ

SpeedOS.LoadAPI("SpeedAPI/config")
SpeedOS.LoadAPI("SpeedAPI/bsod")

-----------------------------------------------------------------------------------------------------------------------------------
-- Какие-то базовые константы, я ебу что это?

pathToConfig = "System/BSOD.settings" -- путь до конфига с всего одним(пока что) параметром для программы
string = config.read(pathToConfig, "string") -- строка, которая будет написана в ошибке 

-----------------------------------------------------------------------------------------------------------------------------------
-- Отрисовка самого БСОДа

bsod.bsod(string)

-----------------------------------------------------------------------------------------------------------------------------------
